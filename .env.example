# ========================================
# LLM 配置 - 選擇以下其中一種方式
# ========================================

# 方式一：使用本地 AI (Ollama) - 推薦！免費且隱私
# 1. 安裝 Ollama: curl -fsSL https://ollama.com/install.sh | sh
# 2. 下載模型: ollama pull llama3.1:8b
# 3. 啟動服務: ollama serve
OPENAI_API_BASE=http://host.docker.internal:11434/v1
LLM_MODEL=llama3.1:8b
OPENAI_API_KEY=ollama  # 使用 Ollama 時填任意值即可

# 方式二：使用 OpenAI API (需要付費)
# OPENAI_API_KEY=sk-your-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1
# LLM_MODEL=gpt-4

# LLM 參數調整
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1000
LLM_NUM_CTX=4096

# Agent 配置
BOT_USERNAME=Agent_001

# Minecraft 伺服器配置
MC_VERSION=1.20.1
MC_DIFFICULTY=normal
MC_MAX_MEMORY=2G
