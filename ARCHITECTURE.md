# Project Observer - 架構圖

## 系統拓撲圖

```
                    🎮 Minecraft Java Client (你)
                             │
                             │ Port 25565
                             ▼
        ┌────────────────────────────────────────────┐
        │      Docker Compose Network (bridge)       │
        │                                            │
        │  ┌─────────────────────────────────────┐  │
        │  │     🎮 Minecraft Server              │  │
        │  │     Container: mc-world              │  │
        │  │     - Fabric 1.20.1                  │  │
        │  │     - Port: 25565                    │  │
        │  └────────────┬────────────────────────┘  │
        │               │                            │
        │               │ Mineflayer Protocol        │
        │               │                            │
        │  ┌────────────▼────────────────────────┐  │
        │  │     🧠 AI Agent Core                 │  │
        │  │     Container: steve-gpt             │  │
        │  │     - Python 3.11 + Node.js 18       │  │
        │  │     ┌─────────────────────────────┐ │  │
        │  │     │ Main Loop (main.py)         │ │  │
        │  │     │  1. Observe → 2. Think      │ │  │
        │  │     │  3. Act → 4. Reflect        │ │  │
        │  │     │  5. Learn → Loop            │ │  │
        │  │     └─────────────────────────────┘ │  │
        │  │     ┌─────────────────────────────┐ │  │
        │  │     │ Bot Controller (bot.js)     │ │  │
        │  │     │  - Mineflayer API           │ │  │
        │  │     │  - Pathfinder               │ │  │
        │  │     └─────────────────────────────┘ │  │
        │  └─────────┬───────────┬───────────────┘  │
        │            │           │                   │
        │      ┌─────▼──────┐    │                  │
        │      │ Skills     │    │                  │
        │      │ Volume     │    │ HTTP API         │
        │      └────────────┘    │                  │
        │                        │                  │
        │            ┌───────────▼──────────────┐   │
        │            │  💾 Vector Database      │   │
        │            │  Container: ai-memory    │   │
        │            │  - ChromaDB              │   │
        │            │  - Port: 8000            │   │
        │            │  - Persistent Storage    │   │
        │            └─────────┬────────────────┘   │
        │                      │                    │
        │                      │ Read Access        │
        │                      │                    │
        │            ┌─────────▼────────────────┐   │
        │            │  📊 Observer Dashboard   │   │
        │            │  Container: dashboard    │   │
        │            │  - Streamlit             │   │
        │            │  - Port: 8501            │   │
        │            └──────────────────────────┘   │
        │                      │                    │
        └──────────────────────┼────────────────────┘
                               │ Port 8501
                               ▼
                        💻 Web Browser (你)
```

## 數據流向圖

```
┌─────────────────────────────────────────────────────┐
│                  Main Evolution Loop                 │
│                                                      │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐   │
│  │ OBSERVE  │────▶│  THINK   │────▶│   ACT    │   │
│  │          │     │          │     │          │   │
│  │ Bot 讀取 │     │ LLM 決策 │     │ 執行代碼 │   │
│  │ 遊戲狀態 │     │          │     │          │   │
│  └────▲─────┘     └────┬─────┘     └────┬─────┘   │
│       │                │                 │         │
│       │                │                 │         │
│       │          ┌─────▼─────┐     ┌────▼─────┐   │
│       │          │  Memory   │     │ REFLECT  │   │
│       │          │  Query    │     │          │   │
│       │          │           │     │ 分析結果 │   │
│       │          └───────────┘     └────┬─────┘   │
│       │                                  │         │
│       │                            ┌─────▼─────┐   │
│       │                            │  LEARN    │   │
│       │                            │           │   │
│       └────────────────────────────┤ 存儲技能  │   │
│                                    │ 和經驗    │   │
│                                    └───────────┘   │
└─────────────────────────────────────────────────────┘
```

## 技能進化流程

```
首次嘗試                    成功執行                    技能複用
   │                          │                          │
   ▼                          ▼                          ▼
┌─────────┐              ┌─────────┐              ┌─────────┐
│ LLM 生成│              │ 執行成功│              │ 直接調用│
│ 新代碼  │──執行───────▶│         │──保存──────▶│ 已有技能│
└─────────┘              └─────────┘              └─────────┘
     │                                                  │
     │ 失敗                                             │ 成功
     ▼                                                  ▼
┌─────────┐                                       ┌─────────┐
│ 分析錯誤│                                       │ 統計累計│
│ 記錄教訓│                                       │ 提升等級│
└────┬────┘                                       └─────────┘
     │
     │ 重新思考
     ▼
┌─────────┐
│ LLM 改進│
│ 策略    │
└────┬────┘
     │
     └──────┐
            │
        再次嘗試
```

## 容器依賴關系

```
啟動順序 (depends_on):

1. mc-server (基礎)
   └── healthcheck: 確保服務器完全啟動

2. chromadb (基礎)
   └── 向量數據庫服務

3. ai-bot (依賴 1 & 2)
   ├── 連接到 mc-server
   └── 連接到 chromadb

4. dashboard (依賴 3)
   └── 讀取 ai-bot 的日誌和數據
```

## 數據持久化

```
Host Machine                    Docker Volumes
─────────────                   ──────────────

./mc-data/           ──────▶    Minecraft 世界數據
                                - 地圖
                                - 玩家數據
                                - 配置

./chroma-data/       ──────▶    向量數據庫
                                - 經驗向量
                                - 失敗案例

./agent_skills/      ──────▶    技能庫
                                - JSON 技能文件
                                - 代碼片段

./agent_logs/        ──────▶    運行日誌
                                - 時間戳日誌
                                - 錯誤記錄

./agent_memory/      ──────▶    記憶數據
                                - 狀態快照
                                - 統計信息
```

## 通訊協議

```
1. Python ↔ Node.js
   方式: stdin/stdout IPC
   格式: JSON
   
   Python → Node.js:
   {"action": "get_state"}
   {"action": "execute_code", "code": "..."}
   
   Node.js → Python:
   {"position": {...}, "health": 20, ...}
   {"success": true, "result": {...}}

2. AI Agent ↔ ChromaDB
   方式: HTTP REST API
   端點: http://chromadb:8000
   
   操作:
   - POST /collections/{name}/query
   - POST /collections/{name}/add

3. Dashboard ↔ Agent
   方式: 文件系統 (只讀)
   
   讀取:
   - /app/logs/*.log
   - /app/skills/*.json
   - /app/memory/*

4. Minecraft Client ↔ Server
   方式: Minecraft Protocol
   端口: 25565
```

## 安全考量

```
隔離層級:

1. 網路隔離
   - 所有容器在私有網路 (ai-net)
   - 只暴露必要端口到主機

2. 文件系統
   - Dashboard 只有只讀權限
   - 技能目錄權限控制

3. API 密鑰
   - 通過環境變量注入
   - 不寫入日誌或代碼

4. 代碼執行
   - 沙盒環境 (容器內)
   - 限制訪問範圍
   - 資源限制 (可在 docker-compose.yml 配置)
```
